server {
    listen 80;
    server_name 167.172.76.154;

    root /usr/share/nginx/html;
    index index.html;

    # This is the key line — redirect all routes to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
